" BASIC SETUP:

" enter the current millenium
set nocompatible

" highlight search
set hls

" line numbering
set number

" tab space value when reading file
set tabstop=4

" tab space value when editing file
set softtabstop=4

" shift width for visual mode indenting
set shiftwidth=4

" replace a tab by its space value
set expandtab

" auto indent
" set autoindent

set encoding=utf-8

" enable syntax and plugins (for netrw)
syntax enable
filetype plugin on

" Colour Scheme
colorscheme slate


set printfont=courier:h8
